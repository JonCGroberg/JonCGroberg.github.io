---
import { Octokit } from "octokit";

const username = "joncgroberg";
const octokit = new Octokit({
    auth: import.meta.env.githubApiKey,
});

const res = await octokit.request(
    "GET /users/{username}/events?per_page=100&page=1",
    {
        username,
    },
    {
        headers: {
            "X-GitHub-Api-Version": "2022-11-28",
        },
    },
);

const response = await fetch(
    "https://github.com/users/joncgroberg/contributions",
);
const contributions = await response.body;
console.log(contributions);

// console.log(res.data.map((event: any) => event.created_at));
---

<div class="container">
    <div class="row">
        <div class="col-md-12"></div>
    </div>
</div>

<script>
    // const response = await fetch("https://github.com/joncgroberg");
    // console.log(response);
</script>
