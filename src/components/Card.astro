---
// Card.astro
// A flexible card component for wrapping content with custom classes
const { title, class: customClass = "", children } = Astro.props;
---

<div class={`bg-white rounded-lg shadow p-6 mb-4 dark:bg-neutral-900 ${customClass}`}>
	{title && <h3 class="font-bold text-lg mb-2 dark:text-neutral-100">{title}</h3>}
	<slot />
</div>
